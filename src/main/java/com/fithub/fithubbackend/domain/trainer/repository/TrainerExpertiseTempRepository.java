package com.fithub.fithubbackend.domain.trainer.repository;

import com.fithub.fithubbackend.domain.trainer.domain.TrainerExpertiseTemp;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TrainerExpertiseTempRepository extends JpaRepository<TrainerExpertiseTemp, Long> {
}
